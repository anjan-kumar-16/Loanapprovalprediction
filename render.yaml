services:
  - type: web
    name: loan-approval-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: FRONTEND_URL
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: loan-approval-db
          property: connectionString

  - type: psql
    name: loan-approval-db
    plan: free
